import styled from 'styled-components'
import { Page as PageBase } from 'pages/subcomponents'

// eslint-disable-next-line import/prefer-default-export
export const Page = styled(PageBase)`
  display: flex;
  justify-content: center;
`
